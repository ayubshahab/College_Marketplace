commands:
    composer_istall:
        command: "composer.phar install --no-dev"
        cwd: /var/app/staging
    migration:
        command: "php /var/app/staging/artisan migrate --force"
        leader_only: true