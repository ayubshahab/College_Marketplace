commands:
    composer_istall:
        command: "php /usr/bin/composer.phar install"
        cwd: "/var/app/staging"
    migration:
        command: "php /var/app/staging/artisan migrate --force"
        leader_only: true